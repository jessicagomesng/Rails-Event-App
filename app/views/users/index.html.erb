<h2>Users List</h2><br>

<div class="index">
    <ul>
    <% @users.each do |user| %>
        <li><%= link_to user.full_name, user_path(user) %></li>
    <% end %>
    </ul>
</div><br>

<% if params[:event_id] %>
    <%= render :partial => "filter" %>
<% end %>
